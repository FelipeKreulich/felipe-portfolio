/**
 * Largura de conteúdo das secções redesenhadas.
 *
 * A hero é full-bleed; as secções novas assentam nesta caixa. As secções
 * antigas ainda vivem no `max-w-4xl` do <main> — à medida que forem
 * refeitas, passam para aqui.
 */
export const SECTION_CONTAINER = "mx-auto w-full max-w-6xl px-8 lg:px-16"
